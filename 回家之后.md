# Mac OS 彻底删除 MySQL
```
sudo rm /usr/local/mysql
sudo rm -rf /usr/local/mysql*
sudo rm -rf /Library/StartupItems/MySQLCOM
sudo rm -rf /Library/PreferencePanes/MySQL*
rm -rf ~/Library/PreferencePanes/MySQL*
sudo rm -rf /Library/Receipts/mysql*
sudo rm -rf /Library/Receipts/MySQL*
```


# Mac OS 下载安装MySQL
- 下载地址：到[官网](https://dev.mysql.com/downloads/mysql/)
- 选择想要的版本
- 直接下载
- 安装就是一路下一步。


# IDEA 创建多个模块

idea中的project就相当于eclipse中的workspace，idea中的module相当于eclipse中的project

先new一个`Empty Project`，然后在项目上新建多个模块。